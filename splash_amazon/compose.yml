version: '3.9'

services:
  splash:
    container_name: splash
    image: scrapinghub/splash
    ports:
      - 8050:8050
    networks:
      - local

networks:
  local:
    name: local
    driver: bridge